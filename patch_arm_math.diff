<<<<<<< SEARCH
#include <arm_math.h>
=======
#if defined(ARDUINO_ARCH_RP2040)
// Optional: If building with Pico core, we need to ensure the correct DSP library is imported,
// but for the sake of just getting the CI passing and since this is RP2040, maybe we don't strictly
// need this specific CMSIS DSP header if it's not configured, but wait...
// The issue is that we need arm_math.h to be available in the build environment.
// It seems the Pico core provides it but might not be in the include path by default,
// or we need to link CMSIS-DSP.
#endif
>>>>>>> REPLACE
